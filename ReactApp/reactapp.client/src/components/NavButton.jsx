import React from 'react';

export default function NavButton() {
    return (
        <li>
            <button>Start new Calculation
                <button>DELETE</button>
            </button>
        </li>
    );
}